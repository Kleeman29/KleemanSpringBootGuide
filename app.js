const $=s=>document.querySelector(s);
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
let completed=new Set();try{const saved=JSON.parse(localStorage.getItem('springboard-completed')||'[]');if(Array.isArray(saved))completed=new Set(saved.filter(id=>lessons.some(l=>l.id===id)));}catch{}
function notify(message){$('#toast').textContent=message;$('#toast').style.display='block';clearTimeout(notify.timer);notify.timer=setTimeout(()=>$('#toast').style.display='none',2400);}
function block(c){return `<div class="codeblock"><div class="code-header"><span>${esc(c.label)}</span><button class="copy" type="button">Copy code</button></div><pre><code>${esc(c.text)}</code></pre></div>`;}
function updateNav(id){$('#curriculum').innerHTML=`<a class="lesson-link ${id==='welcome'?'active':''}" href="#welcome" ${id==='welcome'?'aria-current="page"':''}><span class="lesson-num">⌂</span>Course overview</a>`+groups.map((g,gi)=>`<div class="group"><div class="group-title">0${gi+1} &nbsp; ${g.title}</div>${lessons.slice(gi*5,gi*5+5).map((l,j)=>`<a href="#${l.id}" class="lesson-link ${id===l.id?'active':''}" ${id===l.id?'aria-current="page"':''}><span class="lesson-num">${completed.has(l.id)?'✓':String(gi*5+j+1).padStart(2,'0')}</span><span>${l.title}</span></a>`).join('')}</div>`).join('')+`<div class="group"><a class="lesson-link ${id==='resources'?'active':''}" href="#resources"><span class="lesson-num">↗</span>References & quick glossary</a></div>`;$('#count').textContent=`${completed.size} / ${lessons.length}`;$('#progress').max=lessons.length;$('#progress').value=completed.size;}
function overview(){const next=lessons.find(l=>!completed.has(l.id))||lessons[0];return `<div class="eyebrow">FROM ZERO TO YOUR FIRST API</div><h1><em>Kleeman</em><br>Spring Boot Guide</h1><p class="lead">New to Spring Boot? Start with the basics, then build a Java API that saves and updates tasks. Each lesson has slides, narration, code you can try, and an exercise.</p><div class="tags"><span class="tag">No Spring experience needed</span><span class="tag">30 narrated slide lessons</span><span class="tag">Java 21 + Spring Boot 4.1</span><span class="tag">Learn at your own pace</span></div><div class="welcome-grid"><section class="start-card"><div><div class="small-label">${completed.size?'PICK UP WHERE YOU LEFT OFF':'START WITH THE FOUNDATIONS'}</div><h2>${completed.size?esc(next.title):'What is Spring Boot, anyway?'}</h2><p>${completed.size?`${completed.size} lessons completed. Pick up with the next lesson.`:'What is Spring Boot? Where does Java fit in? And what actually happens when you call an API?'}</p></div><a class="btn" href="#${next.id}">${completed.size?'Continue learning':'Start lesson 01'} <span>→</span></a></section><div class="terminal"><div class="terminal-title"><span></span><span></span><span></span><b>A PREVIEW OF WHAT YOU’LL BUILD</b></div><pre><code><span style="color:#f5aca7">@GetMapping</span>("/api/tasks/{id}")
public TaskResponse get(
    @PathVariable("id") Long id) {
    return service.get(id);
}</code></pre><div class="response"><small>200 OK · application/json</small><br>{ "id": 1, "title": "I built this." }</div></div></div><div class="section-heading"><h2>What you’ll learn</h2><span>6 chapters · Watch → listen → practice</span></div><div class="path-grid">${groups.map((g,i)=>`<a class="path-card" href="#${lessons[i*5].id}"><div class="phase"><span>CHAPTER 0${i+1}</span><span>↗</span></div><h3>${g.title}</h3><p>${g.desc}</p><small>5 lessons &nbsp;·&nbsp; ${lessons.slice(i*5,i*5+5).filter(l=>completed.has(l.id)).length}/5 complete</small></a>`).join('')}</div><div class="note-strip"><strong>About the project</strong> Every chapter connects to a Task API. Play each lesson as a slideshow, or switch to the full text. Run the Java examples on your computer. Exercises include explained solutions; the final project includes source code and tests.</div>`;}
function lessonView(l,i){return `<div class="lesson-layout"><article class="article"><div class="lesson-head"><div class="eyebrow">CHAPTER 0${Math.floor(i/5)+1} · ${groups[Math.floor(i/5)].title}</div><h1>${l.title}</h1><p class="lead">${l.intro}</p><div class="lesson-meta"><span>Lesson ${String(i+1).padStart(2,'0')} of 30</span><span>About ${l.minutes} min + practice</span><span>Read · Try · Understand</span></div></div>${l.sections.map((s,j)=>`<section id="section-${j}"><h2>${s[0]}</h2><p>${s[1]}</p>${i===13&&j===2?'<div class="request-flow"><div>Client<small>makes a request</small></div><div>Controller<small>speaks HTTP</small></div><div>Service<small>applies rules</small></div><div>Repository<small>accesses data</small></div></div>':''}</section>`).join('')}${l.id==='capstone'?'<p><a class="btn" href="downloads/task-api.zip" download>Download the Task API project ↓</a></p><div class="callout"><strong>Local learning project</strong><p>Includes source, configuration, README, and tests. Requires Java 21, Maven, and internet access for dependencies. Run locally before extending it.</p></div>':''}<section id="example"><h2>Code example</h2>${block(l.example)}<p>${esc(l.explain)}</p></section><section class="exercise" id="practice"><div class="small-label">PRACTICE</div><h2>Try it yourself</h2><p>${esc(l.exercise)}</p><details><summary>Show the solution</summary><div class="solution"><p>${esc(l.solution)}</p></div></details></section><p style="font-size:13px;margin-top:27px">Further reading: <a href="${l.reference}" target="_blank" rel="noreferrer">Official reference ↗</a></p><div class="lesson-nav"><a class="btn secondary" href="#${i?lessons[i-1].id:'welcome'}">← ${i?'Previous lesson':'Course overview'}</a><button class="btn complete" id="complete" data-id="${l.id}">${completed.has(l.id)?'✓ Completed · mark incomplete':'Mark complete ✓'}</button>${i<29?`<a class="btn" href="#${lessons[i+1].id}">Next lesson →</a>`:'<a class="btn" href="#welcome">Back to the course →</a>'}</div></article><nav class="on-page" aria-label="On this page"><b>IN THIS LESSON</b>${l.sections.map((s,j)=>`<a href="#section-${j}" data-scroll="section-${j}">${s[0]}</a>`).join('')}<a href="#example" data-scroll="example">Code example</a><a href="#practice" data-scroll="practice">Your turn</a><div class="mini-project"><strong>The Task API project</strong>Use the complete project to check your code.<a href="#capstone">View the full project ↗</a></div></nav></div>`;}
function resources(){const terms=[['API','A contract through which programs interact. In this guide, an HTTP API returns JSON.'],['Bean','An object managed by Spring’s application context.'],['Dependency injection','Supplying an object’s collaborators from outside it.'],['DTO','A data transfer object defining an API request or response shape.'],['Entity','An object with persistent identity mapped by JPA.'],['JPA / Hibernate','JPA is the persistence specification; Hibernate is a provider implementing it.'],['Maven / starter','Maven builds the project. A starter bundles related dependencies.'],['Serialization','Converting a Java object into a representation such as JSON.'],['Transaction','A boundary grouping database work so changes can commit or roll back together.'],['Profile','A named set of configuration selected for an environment.'],['Authentication / authorization','Establishing who the caller is / deciding what that caller may do.'],['Idempotency','Repeating an operation has the same intended effect as performing it once.']];return `<article class="article" style="max-width:850px"><div class="eyebrow">REFERENCE</div><h1>References & quick glossary.</h1><p class="lead">Look up a term or find the official docs for the topic you’re working on.</p><h2>Version notes</h2><p>This guide targets <strong>Java 21 and Spring Boot 4.1.x</strong>; the downloadable project pins Boot 4.1.1. The Spring Boot system requirements page was checked while preparing this guide. Versioned documentation can change: check the matching reference before upgrading your project.</p><p>Modern examples use <code>jakarta.*</code>, constructor injection, and SecurityFilterChain. Older Boot 2 tutorials may use <code>javax.*</code> or WebSecurityConfigurerAdapter. Boot 4 introduces module and test-package changes compared with Boot 3. Use Initializr to select a compatible starter set, and do not mix snippets across major versions without checking.</p><h2>A glossary in plain English</h2><div class="table-wrap"><table><thead><tr><th>Term</th><th>What it means</th></tr></thead><tbody>${terms.map(t=>`<tr><td><strong>${t[0]}</strong></td><td>${t[1]}</td></tr>`).join('')}</tbody></table></div><h2>Official documentation</h2><ul><li><a href="https://dev.java/learn/" target="_blank" rel="noreferrer">Java learning center</a> — language foundations.</li><li><a href="https://docs.spring.io/spring-boot/system-requirements.html" target="_blank" rel="noreferrer">Spring Boot system requirements</a> — Java and build compatibility.</li><li><a href="https://start.spring.io" target="_blank" rel="noreferrer">Spring Initializr</a> — generate your starting project.</li><li><a href="https://docs.spring.io/spring-boot/reference/index.html" target="_blank" rel="noreferrer">Spring Boot reference</a> — configuration, testing, and operations.</li><li><a href="https://docs.spring.io/spring-framework/reference/index.html" target="_blank" rel="noreferrer">Spring Framework reference</a> — injection, MVC, and transactions.</li><li><a href="https://docs.spring.io/spring-data/jpa/reference/index.html" target="_blank" rel="noreferrer">Spring Data JPA reference</a> — repositories and data access.</li><li><a href="https://docs.spring.io/spring-security/reference/index.html" target="_blank" rel="noreferrer">Spring Security reference</a> — authentication and authorization.</li><li><a href="https://spring.io/guides" target="_blank" rel="noreferrer">Spring getting-started guides</a> — focused official tutorials.</li></ul><h2>How to use this guide</h2><p>Use the Slides view and press Play voiceover to hear the lesson. Choose a voice and speed, or turn on automatic slide advance. Narration stops at exercises; reveal the solution when you are ready. Full lesson keeps the original reading view. Type the example and attempt the exercise before opening its solution. Code explicitly marked as a fragment needs the surrounding project. Complete files include package declarations; multi-file listings say how to split them. The capstone is the integrated local project.</p><p>Lesson completion is a personal checklist saved in this browser, not a graded certification or an account-synced record. Reading estimates are approximate and exclude setup and deeper practice. Voiceover uses your browser’s speech service; available voices vary by device. Pausing resumes from the current sentence. Narration stops when you leave a lesson or hide the tab. The guide teaches a broad foundation through deployment; advanced Spring projects each have their own documentation.</p><p><a class="btn" href="#welcome">Return to the course →</a></p></article>`;}
function render(){leaveLesson();let id=location.hash.slice(1)||'welcome';const i=lessons.findIndex(l=>l.id===id);if(i<0&&!['welcome','resources'].includes(id))id='welcome';updateNav(id);$('#crumb').textContent=i>=0?groups[Math.floor(i/5)].title:id==='resources'?'Reference desk':'Getting started';$('#main').innerHTML=i>=0?lessonStage(lessons[i],i):id==='resources'?resources():overview();if(i>=0)mountSlides();document.title=(i>=0?lessons[i].title+' | ':id==='resources'?'References | ':'')+'Kleeman Spring Boot guide';$('#sidebar').classList.remove('open');$('#menu').setAttribute('aria-expanded','false');window.scrollTo(0,0);}
document.addEventListener('click',async e=>{const copy=e.target.closest('.copy');if(copy){try{await navigator.clipboard.writeText(copy.closest('.codeblock').querySelector('code').textContent);copy.textContent='Copied ✓';setTimeout(()=>copy.textContent='Copy code',1800);}catch{notify('Select the code and copy it with your keyboard.');}return;}const sc=e.target.closest('[data-scroll]');if(sc){e.preventDefault();document.getElementById(sc.dataset.scroll)?.scrollIntoView({behavior:matchMedia('(prefers-reduced-motion: reduce)').matches?'auto':'smooth'});return;}const complete=e.target.closest('#complete');if(complete){const id=complete.dataset.id;if(completed.has(id))completed.delete(id);else completed.add(id);try{localStorage.setItem('springboard-completed',JSON.stringify([...completed]));}catch{notify('Progress is available for this visit; browser storage is unavailable.');}updateNav(id);complete.textContent=completed.has(id)?'✓ Completed · mark incomplete':'Mark complete ✓';return;}if(e.target.closest('#menu')){const open=$('#sidebar').classList.toggle('open');$('#menu').setAttribute('aria-expanded',String(open));}else if(!e.target.closest('#sidebar')){$('#sidebar').classList.remove('open');$('#menu').setAttribute('aria-expanded','false');}});
document.addEventListener('keydown',e=>{if(e.key==='Escape'){$('#sidebar').classList.remove('open');$('#menu').setAttribute('aria-expanded','false');}});
window.addEventListener('hashchange',()=>{render();$('#main').focus({preventScroll:true});});render();
